首先是相机基本处理
（大恒有ROS否，无则打包）
需求在哪？新UI？
如何实现UI显示？
下位机是何意味？
通信协议（代码）MQTT，类ROS2逻辑


首先是整体组织的基本流程：以GXU_RM为例。
引入库主要是Qt相关。

> [!abstract] 流程总结
> - 首先初始化网络与解码层（创建指针？），调用VideoDecoder定义的函数串流（官方UDP HEVC流与自定义客户端MQTT H264流）
> - 绑定UDP数据流到解码线程
> - 绑定MQTT 0x0310 自定义数据流到自定义图传解码线程（connect函数?并额外调用Qt::QueuedConnection）
> - 绑定解码线程输出到主线程渲染（Qt::QueuedConnection）
> - 启动解码线程(m_videoDecoder与 m_customVideoDecoder)并赋予高线程优先级，保障其吞吐能力(将指针传入start(QThread::HighPrioety))
> - 鼠标滑动追踪API（QWidget::setMouseTracking？ ）
> - 启动（QTimer自带API？）定时器以定频发包




> [!info] 相机视频编码与解码
> 
